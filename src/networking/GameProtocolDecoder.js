'use strict';

//now stub but must invoke callback with decoded options for this action
function decodeEnemyParams(json){
    return {};
}

//now stub but must invoke callback with decoded options for this action
function decodeMoveParams(json){
    return {};
}

//now stub but must return with decoded options for this action
function decodeShootParams(json){
    return {};
}


module.exports = {
    decodeEnemyParams : decodeEnemyParams,
    decodeMoveParams : decodeMoveParams,
    decodeShootParams : decodeShootParams
};
